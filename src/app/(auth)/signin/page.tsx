import SigninForm from "@/components/auth/signin-form";

const SigninPage = () => {
  return (
    <main className="min-h-screen max-w-[440px] mx-auto">
      <SigninForm />
    </main>
  );
};

export default SigninPage;
